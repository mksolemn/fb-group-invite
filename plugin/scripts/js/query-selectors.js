'use strict';

(function ($) {
    $(document).ready(function () {
        var queryModule = {
            querySelectors: function querySelectors() {
                console.log('query selector running...');
            }

        };
    });
})(jQuery);