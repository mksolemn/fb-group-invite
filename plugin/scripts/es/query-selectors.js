(function ($) {
    $(document).ready(function () {
        let queryModule = {
            querySelectors: () => {
                console.log('query selector running...');
            }

        }

    })
})(jQuery);